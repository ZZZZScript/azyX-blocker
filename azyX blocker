-- Services
local TweenService = game:GetService("TweenService")
local HttpService = game:GetService("HttpService")
local CoreGui = game:GetService("CoreGui")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Workspace = game:GetService("Workspace")

-- Founder / Job Finder Logic
task.spawn(function()
    for _, label in ipairs(CoreGui:GetDescendants()) do
        if label:IsA("TextLabel") and label.Text:match("Job%-ID Input") then
            local parent = label.Parent
            local grandParent = parent.Parent

            for _, box in ipairs(grandParent:GetDescendants()) do
                if box:IsA("TextBox") then
                    for _, button in ipairs(grandParent:GetDescendants()) do
                        if button:IsA("TextButton") and button.Text:match("Join Job%-ID") then
                            -- Original logic: you could automate or track here
                            -- Example: print(button.Name)
                        end
                    end
                end
            end
        end
    end
end)

-- Color Definitions
local BG_COLOR = Color3.fromRGB(12, 12, 18)
local FRAME_COLOR = Color3.fromRGB(20, 25, 35)
local ACCENT_COLOR = Color3.fromRGB(0, 255, 255)
local TEXT_COLOR = Color3.fromRGB(255, 255, 255)

-- Color Sequence for Gradients
local gradientKeys = {
    ColorSequenceKeypoint.new(0, ACCENT_COLOR),
    ColorSequenceKeypoint.new(0.5, TEXT_COLOR),
    ColorSequenceKeypoint.new(1, ACCENT_COLOR)
}

-- Main UI
local gui = Instance.new("ScreenGui")
gui.Name = "IceFinderUI_v49_PerfectVisuals"
gui.ResetOnSpawn = false
gui.Parent = gethui()

-- Overlay Stats (FPS / Ping)
local overlay = Instance.new("Frame")
overlay.Name = "OverlayStats"
overlay.Size = UDim2.new(0, 200, 0, 30)
overlay.Position = UDim2.new(0.5, -100, 0, 5)
overlay.BackgroundTransparency = 1
overlay.Parent = gui

local overlayLayout = Instance.new("UIListLayout")
overlayLayout.FillDirection = Enum.FillDirection.Horizontal
overlayLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
overlayLayout.Padding = UDim.new(0, 10)
overlayLayout.Parent = overlay

-- FPS Frame
local fpsFrame = Instance.new("Frame")
fpsFrame.Size = UDim2.new(0, 80, 0, 25)
fpsFrame.BackgroundColor3 = BG_COLOR
fpsFrame.BackgroundTransparency = 0.3
fpsFrame.Visible = false
fpsFrame.Parent = overlay
Instance.new("UICorner", fpsFrame).CornerRadius = UDim.new(0, 6)

local fpsLabel = Instance.new("TextLabel")
fpsLabel.Size = UDim2.new(1, 0, 1, 0)
fpsLabel.BackgroundTransparency = 1
fpsLabel.TextColor3 = TEXT_COLOR
fpsLabel.Font = Enum.Font.GothamBold
fpsLabel.TextSize = 14
fpsLabel.Text = "FPS: 0"
fpsLabel.Parent = fpsFrame

local fpsStroke = Instance.new("UIStroke")
fpsStroke.Thickness = 1.5
fpsStroke.Color = ACCENT_COLOR
fpsStroke.Parent = fpsFrame

local fpsGradient = Instance.new("UIGradient")
fpsGradient.Color = ColorSequence.new(gradientKeys)
fpsGradient.Rotation = 45
fpsGradient.Parent = fpsStroke

-- Ping Frame (same style as FPS)
local pingFrame = fpsFrame:Clone()
pingFrame.Parent = overlay
pingFrame.Visible = false

local pingLabel = pingFrame:FindFirstChildOfClass("TextLabel")
pingLabel.Text = "Ping: 0"

-- Main Frame
local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Size = UDim2.new(0, 340, 0, 380)
mainFrame.Position = UDim2.new(0.5, -170, 0.5, -190)
mainFrame.BackgroundColor3 = BG_COLOR
mainFrame.BorderSizePixel = 0
mainFrame.ClipsDescendants = true
mainFrame.Parent = gui
Instance.new("UICorner", mainFrame).CornerRadius = UDim.new(0, 26)

local mainStroke = Instance.new("UIStroke")
mainStroke.Thickness = 3.5
mainStroke.Color = TEXT_COLOR
mainStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
mainStroke.Parent = mainFrame

-- Animated Gradient on Border
local mainGradient = Instance.new("UIGradient")
mainGradient.Color = ColorSequence.new(ACCENT_COLOR)
mainGradient.Rotation = 45
mainGradient.Parent = mainStroke

RunService.RenderStepped:Connect(function()
    mainGradient.Rotation += 3
end)

-- Header
local header = Instance.new("Frame")
header.Size = UDim2.new(1, 0, 0, 50)
header.BackgroundTransparency = 1
header.ZIndex = 10
header.Parent = mainFrame

local titleLabel = Instance.new("TextLabel")
titleLabel.Text = "Ice Finder"
titleLabel.Font = Enum.Font.GothamBlack
titleLabel.TextSize = 16
titleLabel.TextColor3 = TEXT_COLOR
titleLabel.TextXAlignment = Enum.TextXAlignment.Left
titleLabel.BackgroundTransparency = 1
titleLabel.Position = UDim2.new(0, 30, 0, 0)
titleLabel.Size = UDim2.new(0.5, 0, 1, 0)
titleLabel.Parent = header

-- Filter Button
local filterBtn = Instance.new("TextButton")
filterBtn.Size = UDim2.new(0, 80, 0, 28)
filterBtn.Position = UDim2.new(1, -125, 0.5, -14)
filterBtn.BackgroundColor3 = FRAME_COLOR
filterBtn.Text = "🔍 Filter"
filterBtn.TextColor3 = TEXT_COLOR
filterBtn.Font = Enum.Font.GothamBold
filterBtn.TextSize = 12
filterBtn.Parent = header
Instance.new("UICorner", filterBtn).CornerRadius = UDim.new(0, 8)

-- Close Button
local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 28, 0, 28)
closeBtn.Position = UDim2.new(1, -38, 0.5, -14)
closeBtn.BackgroundColor3 = FRAME_COLOR
closeBtn.Text = "X"
closeBtn.TextColor3 = TEXT_COLOR
closeBtn.Font = Enum.Font.GothamBlack
closeBtn.TextSize = 14
closeBtn.Parent = header
Instance.new("UICorner", closeBtn).CornerRadius = UDim.new(0, 8)

closeBtn.MouseButton1Click:Connect(function()
    gui:Destroy()
end)

-- Scrollable Results Container
local scrollContainer = Instance.new("Frame")
scrollContainer.Name = "ScrollContainer"
scrollContainer.Size = UDim2.new(1, -20, 1, -100)
scrollContainer.Position = UDim2.new(0, 10, 0, 80)
scrollContainer.BackgroundTransparency = 1
scrollContainer.ClipsDescendants = true
scrollContainer.Parent = mainFrame

local scroll = Instance.new("ScrollingFrame")
scroll.Name = "LogScroll"
scroll.Size = UDim2.new(1, 0, 1, 0)
scroll.BackgroundTransparency = 1
scroll.ScrollBarThickness = 3
scroll.ScrollBarImageColor3 = ACCENT_COLOR
scroll.BorderSizePixel = 0
scroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
scroll.CanvasSize = UDim2.new(0, 0, 0, 0)
scroll.Parent = scrollContainer

local scrollLayout = Instance.new("UIListLayout")
scrollLayout.SortOrder = Enum.SortOrder.LayoutOrder
scrollLayout.Padding = UDim.new(0, 4)
scrollLayout.Parent = scroll
